# execution-tool
sandboxed tool execution for agents — shell, HTTP, filesystem, database — and the HTTP destination policy is the part that matters. Most agent frameworks ship naive URL validation that a DNS   rebind or a redirect to 169.254.169.254 walks straight through.
