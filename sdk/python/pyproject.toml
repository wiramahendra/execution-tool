[project]
name = "execution-tool-sdk"
version = "0.1.0"
description = "Python SDK for executiond — filesystem/shell/http with SSRF + sandbox policy"
readme = "README.md"
requires-python = ">=3.9"
dependencies = ["requests>=2.31", "httpx>=0.24"]
license = {text = "MIT"}

[project.optional-dependencies]
dev = ["pytest"]
